#
if(NUMERIC_BACKEND STREQUAL "IQMATH")
    set(User_SOURCES
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/fixed_control_gateway.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/foc_iq.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/identification_iq.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/mtpa_iq.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/initialization_iq.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/motor_iq_core.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/motor_iq_gateway.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/protect_iq_core.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/protect_iq_gateway.c
    )
else()
    set(User_SOURCES
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/motor.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/protect.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/initialization.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/foc.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/identification.c
        ${CMAKE_CURRENT_SOURCE_DIR}/Src/MTPA.c
    )
endif()

add_library(User STATIC
    ${User_SOURCES}
)

if(NUMERIC_BACKEND STREQUAL "IQMATH")
    target_compile_options(User PRIVATE -fstack-usage)
endif()

target_include_directories(User PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/Inc
)

add_library(Application INTERFACE)

target_link_libraries(Application
    INTERFACE User
)

target_link_libraries(User PUBLIC
    Core
    Middleware
    Communication
    Drivers
    Utils
)
